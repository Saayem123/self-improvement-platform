// App constants
export const APP_NAME = 'Behavior Tracker';

// localStorage keys
export const STORAGE_KEYS = {
  USERS: 'users',
  BEHAVIORS: 'behaviors',
  USER: 'user',
};

// Default colors for behaviors
export const DEFAULT_COLORS = [
  'indigo', 'blue', 'green', 'yellow', 'red', 'purple', 'pink',
];

// Animation timing
export const ANIMATION_DURATION = {
  short: 200,
  medium: 300,
  long: 500,
};